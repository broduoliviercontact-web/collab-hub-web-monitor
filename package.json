{
  "name": "ch-web-monitor-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/runTests.mjs",
    "check": "npm test && node max/validate-maxpat.mjs && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  }
}