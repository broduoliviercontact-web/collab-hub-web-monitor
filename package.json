{
  "name": "collab-hub-web-monitor",
  "private": true,
  "version": "1.1.2",
  "type": "module",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/runTests.mjs",
    "check:license": "node scripts/check-license.mjs",
    "check:tracked": "node scripts/check-tracked-files.mjs",
    "check:secrets": "node scripts/check-livekit-secrets.mjs",
    "check": "npm test && npm run check:license && npm run check:tracked && npm run check:secrets && node max/validate-maxpat.mjs && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "livekit-client": "^2.20.1",
    "livekit-server-sdk": "^2.17.0",
    "socket.io-client": "^4.7.5"
  }
}
